services:
  - type: web
    name: wasify-backend
    env: docker
    dockerfilePath: ./Dockerfile
    dockerCommand: "uvicorn api.main:app --host 0.0.0.0 --port 10000"
    envVars:
      - key: PORT
        value: 10000
      - key: WASIFY_AUTH_SECRET
        generateValue: true
